import 'package:flutter/material.dart';

class SportsNewsListScreen extends StatelessWidget {
  const SportsNewsListScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }


}