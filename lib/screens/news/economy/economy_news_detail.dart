import 'package:flutter/material.dart';

class EconomyNewsDetailScreen extends StatelessWidget {
  const EconomyNewsDetailScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}