import 'package:flutter/material.dart';

class EconomyNewsListScreen extends StatelessWidget {
  const EconomyNewsListScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}