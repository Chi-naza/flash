import 'package:flutter/material.dart';

class ArtNewsListScreen extends StatelessWidget {
  const ArtNewsListScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}