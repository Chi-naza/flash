import 'package:flutter/material.dart';

class ArtNewsDetailScreen extends StatelessWidget {
  const ArtNewsDetailScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}